# راهنمای آپلود فولدر فایل‌های Word

## دو روش برای آپلود فولدر

### روش 1: آپلود فایل ZIP (پیشنهادی) 🎯

این روش برای زمانی است که می‌خواهید فولدر را از کامپیوتر خود آپلود کنید.

#### گام 1: ساخت فایل ZIP
1. فولدر حاوی فایل‌های Word را انتخاب کنید
2. روی آن راست کلیک کرده و "Send to" > "Compressed (zipped) folder" را انتخاب کنید
   - یا از نرم‌افزارهای مثل WinRAR، 7-Zip استفاده کنید

#### گام 2: آپلود ZIP
از Swagger UI یا Postman:
```
POST /rag/upload-folder-zip
Content-Type: multipart/form-data

Body (form-data):
  zip_file: [انتخاب فایل ZIP]
```

**پاسخ:**
```json
{
  "status": "success",
  "message": "فایل‌های Word با موفقیت پردازش و به vectordb اضافه شدند",
  "files_processed": 25,
  "chunks_added": 150
}
```

### روش 2: پردازش از مسیر محلی سرور

این روش برای زمانی است که فایل‌ها از قبل در سرور موجود هستند.

```
POST /rag/upload-folder-from-path
Content-Type: application/json

Body:
{
  "folder_path": "data/ghavanin",
  "recursive": true
}
```

**پارامترها:**
- `folder_path`: مسیر فولدر (نسبی به BASE_DIR یا مطلق)
- `recursive`: آیا در زیرفولدرها هم جستجو شود (پیش‌فرض: true)

**پاسخ:**
```json
{
  "status": "success",
  "message": "فایل‌های Word با موفقیت پردازش و به vectordb اضافه شدند",
  "folder_path": "/path/to/folder",
  "files_processed": 25,
  "chunks_added": 150
}
```

## مثال‌های استفاده

### مثال 1: آپلود ZIP با Postman

1. Postman را باز کنید
2. Method: `POST`
3. URL: `http://localhost:8000/rag/upload-folder-zip`
4. در تب "Body"، "form-data" را انتخاب کنید
5. Key: `zip_file` (نوع: File)
6. Value: فایل ZIP خود را انتخاب کنید
7. Send را بزنید

### مثال 2: استفاده از curl

```bash
curl -X POST "http://localhost:8000/rag/upload-folder-zip" \
  -F "zip_file=@/path/to/ghavanin.zip"
```

### مثال 3: پردازش فولدر از مسیر سرور

```bash
curl -X POST "http://localhost:8000/rag/upload-folder-from-path" \
  -H "Content-Type: application/json" \
  -d '{
    "folder_path": "data/ghavanin",
    "recursive": true
  }'
```

## نکات مهم

✅ **فقط فایل‌های Word پردازش می‌شوند** (.docx, .doc)  
✅ **پردازش بازگشتی**: اگر `recursive=true` باشد، در تمام زیرفولدرها هم جستجو می‌شود  
✅ **پردازش دسته‌ای**: تمام فایل‌ها به صورت دسته‌ای پردازش شده و به vectordb اضافه می‌شوند  
✅ **مدیریت خطا**: اگر یک فایل خطا دهد، بقیه فایل‌ها همچنان پردازش می‌شوند  
✅ **Metadata**: نام فایل به عنوان source در metadata ذخیره می‌شود

## بررسی نتیجه

بعد از آپلود، می‌توانید با endpoint زیر آمار vectordb را ببینید:

```
GET /rag/stats
```

## مثال کامل

```python
# آپلود ZIP
import requests

with open("ghavanin.zip", "rb") as f:
    response = requests.post(
        "http://localhost:8000/rag/upload-folder-zip",
        files={"zip_file": f}
    )
    print(response.json())
    # {
    #   "status": "success",
    #   "files_processed": 25,
    #   "chunks_added": 150
    # }
```

## عیب‌یابی

### مشکل: "فایل ZIP معتبر نیست"
- ✅ مطمئن شوید فایل واقعاً ZIP است
- ✅ فایل را با WinRAR یا 7-Zip باز کنید و ببینید که معتبر است

### مشکل: "هیچ فایل Word معتبری پیدا نشد"
- ✅ مطمئن شوید فایل‌ها دارای پسوند .docx یا .doc هستند
- ✅ بررسی کنید که فایل‌ها در فولدر اصلی ZIP هستند (نه در یک فولدر داخل ZIP)

### مشکل: خطا در پردازش
- ✅ لاگ‌های سرور را بررسی کنید
- ✅ مطمئن شوید فایل‌های Word معتبر و قابل خواندن هستند

